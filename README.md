# trajectory-optimizer
This project aims to explore the simulated annealing local search algorithm on a 2D game to find the optimal moves in a given level. The game is modelled as a continuous optimization task on a mini golf environment with basic rules, in which the agent has to find the optimal shots within a range of angle and force. 
